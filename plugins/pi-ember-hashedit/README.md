# pi-ember-hashedit

Hash-anchored `read`, `replace`, and `undo_last_replace` tools for Pi.

Vendored from `pi-hashline-edit-pro` (MIT) and adapted to the Ember stack.

Every line gets a unique 3-character hash. Edits target hashes, not line numbers or fuzzy context.
